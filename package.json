{
  "name": "nowworksai-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3001",
    "build": "echo 'No build step required — static site'",
    "deploy": "npx wrangler pages deploy ./ --project-name=nowworksai-site --branch=main"
  }
}
